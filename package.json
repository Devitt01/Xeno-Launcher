{
  "name": "xeno-launcher",
  "version": "1.0.0",
  "description": "Xeno Launcher",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "clean:dist": "node -e \"const fs=require('fs');['dist','dist-release'].forEach(d=>fs.rmSync(d,{recursive:true,force:true}));\"",
    "build:asar-asset": "node scripts/export-update-asar.js",
    "build:win": "electron-builder --win nsis --publish never",
    "build:win:clean": "npm run clean:dist && electron-builder --win nsis --publish never",
    "build:portable": "npm run clean:dist && electron-builder --win portable --publish never --config.win.artifactName=XenoLauncher-Portable-${version}.exe",
    "build:release:win": "set \"XENO_BUILD_OUTPUT_DIR=dist-release\" && electron-builder --win nsis --publish never --config.directories.output=dist-release && npm run build:asar-asset && electron-builder --win portable --publish never --config.directories.output=dist-release --config.win.artifactName=XenoLauncher-Portable-${version}.exe",
    "build:patch:asar": "set \"XENO_BUILD_OUTPUT_DIR=dist-release\" && electron-builder --win --dir --publish never --config.directories.output=dist-release && npm run build:asar-asset"
  },
  "author": "Devitt",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/Devitt01/Xeno-Launcher.git"
  },
  "dependencies": {
    "electron-updater": "^6.3.0",
    "minecraft-launcher-core": "^3.14.0",
    "electron-store": "^8.1.0"
  },
  "devDependencies": {
    "electron": "^22.0.0",
    "electron-builder": "^24.13.3"
  },
  "build": {
    "appId": "com.xeno.launcher",
    "productName": "Xeno Launcher",
    "icon": "build/icon.ico",
    "directories": {
      "output": "dist-release"
    },
    "files": [
      "**/*",
      "!dist/**",
      "!node_modules/.cache/**",
      "!cloudflared.exe",
      "!*.log",
      "!dist-release/**"
    ],
    "win": {
      "icon": "build/icon.ico",
      "target": "nsis",
      "artifactName": "XenoLauncher-Setup-${version}.exe"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "perMachine": false,
      "deleteAppDataOnUninstall": false,
      "installerIcon": "build/icon.ico",
      "uninstallerIcon": "build/icon.ico",
      "installerHeaderIcon": "build/icon.ico"
    },
    "publish": [
      {
        "provider": "github",
        "owner": "Devitt01",
        "repo": "Xeno-Launcher",
        "releaseType": "release"
      }
    ]
  }
}
